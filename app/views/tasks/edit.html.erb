<%= form_for @task, :url => { :controller => "tasks", :action => "update" } do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label "Done?" %>
  <%= f.check_box :status %>

  <%= f.submit %>
<% end %>
